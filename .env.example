# REDSWARM — Umgebungsvariablen
# ==========================================
# Kopiere diese Datei zu .env und fülle die Werte aus:
#   cp .env.example .env
#   docker compose up -d

# ── Dashboard & Backend ──────────────────────
# API-Key fuer Agent↔Backend Kommunikation (frei waehlbar)
REDSWARM_API_KEY=changeme-to-a-secure-random-string

# Port fuer den Nginx Reverse Proxy (Standard: 80)
# REDSWARM_PORT=80

# ── KI-Provider (mindestens einen angeben) ───
# OpenAI
OPENAI_API_KEY=sk-DEIN_OPENAI_KEY_HIER

# Anthropic (optional, falls Claude-basierte Agents)
# ANTHROPIC_API_KEY=sk-ant-DEIN_KEY_HIER

# ── Notion Integration (optional) ────────────
# NOTION_API_KEY=ntn_DEIN_NOTION_API_KEY_HIER
# NOTION_DATABASE_ID=deine-notion-datenbank-id-hier

# ── Webhook (optional) ───────────────────────
# WEBHOOK_SECRET=dein-geheimes-webhook-passwort

# ── Cognitive Layer (LLM-Reasoning, Memory) ───
# Provider: anthropic oder openai
REDSWARM_LLM_PROVIDER=anthropic
# Model: leer = auto-select (claude-sonnet-4 / gpt-4o)
# REDSWARM_LLM_MODEL=
REDSWARM_LLM_TEMPERATURE=0.7
REDSWARM_LLM_MAX_TOKENS=2000
# Cognitive Layer deaktivieren (spart API-Kosten):
# REDSWARM_COGNITIVE_ENABLED=false

# ── Swarm Intelligence ─────────────────────────
# Heartbeat-Timeout in Sekunden (Agent gilt als tot)
REDSWARM_HEARTBEAT_TIMEOUT=30.0
# Pheromone Decay Rate (0.0-1.0, hoeher = schnellerer Verfall)
REDSWARM_PHEROMONE_DECAY=0.05

# ── 3-Tier Payload Taxonomy (v1.3) ──────────────
# Tiers einzeln aktivieren/deaktivieren:
REDSWARM_TIER1_ENABLED=true
REDSWARM_TIER2_ENABLED=true
REDSWARM_TIER3_ENABLED=true
# Automatische Tier-Auswahl basierend auf Kontext:
REDSWARM_TIER_AUTO_SELECT=true
# Tier-1-Payloads beim Start in Knowledge Base laden:
# REDSWARM_LOAD_TIER1_INTO_KB=true
# Max. Polymorphic-Mutationen pro Payload (Tier 2):
REDSWARM_MAX_TIER2_MUTATIONS=10
# Min. Findings bevor Tier 3 aktiviert wird:
REDSWARM_TIER3_MIN_FINDINGS=3

# ── Anti-Hallucination / Validation Layer ───────
# Validation komplett deaktivieren:
# REDSWARM_VALIDATION_ENABLED=false
# Payloads vor Ausfuehrung validieren:
REDSWARM_VALIDATE_PAYLOADS=true
# Max. Payload-Laenge (Zeichen):
REDSWARM_MAX_PAYLOAD_LENGTH=10000
# Ergebnisse nach Ausfuehrung verifizieren:
REDSWARM_VERIFY_RESULTS=true
# Min. Evidenz-Punkte fuer CONFIRMED-Status:
REDSWARM_MIN_EVIDENCE_COUNT=2
# Bei Erfolg automatisch Re-Test ausfuehren:
# REDSWARM_RETEST_ON_SUCCESS=true
# Empirische Confidence statt LLM-Halluzination:
REDSWARM_USE_EMPIRICAL_CONFIDENCE=true
# Confidence Decay Rate (pro Stunde ohne Re-Verifikation):
REDSWARM_CONFIDENCE_DECAY_RATE=0.1
# Min. Confidence Threshold:
REDSWARM_MIN_CONFIDENCE_THRESHOLD=0.3
# Multi-Agent-Konsens fuer Findings:
REDSWARM_REQUIRE_CONSENSUS=true
# Min. Agenten fuer Konsens-Quorum:
REDSWARM_CONSENSUS_QUORUM=2
# Ab welcher Severity Konsens erforderlich (low/medium/high/critical):
REDSWARM_CONSENSUS_MIN_SEVERITY=medium

# ── Standard-Ziel fuer API-Tests (optional) ──
# DEFAULT_API_KEY=sk-xxxxx
# DEFAULT_API_URL=https://api.openai.com/v1
# DEFAULT_MODEL=gpt-4
